import React from 'react'
import { Layout } from 'antd'

export const Footer = () => {
    const { Footer } = Layout;
    return (
        <Footer style={{ textAlign: "center" }}>
            Creado por Joel Blanco
        </Footer>
    )
}
